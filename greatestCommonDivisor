// // num2 must be greater that num1

// function greatestCommonDivisor(num1, num2){
//   var divisors = [];
//   for(i = 2; i <= num1; i++){
//     if(num1 % i == 0 && num2 % i == 0){
//       divisors.push(i);
//     }
//   }
//   return divisors[divisors.length-1];
// }

// easier way:
function greatestCommonDivisor(num1, num2){
  if(num2 == 0){
    return "do not divide through zero";
  } else{
    return num1%num2; 
  }
}
